{
  "enabled": {
    "description": "启用内测指令门禁",
    "type": "bool",
    "default": true,
    "hint": "关闭后不拦截任何指令。"
  },
  "beta_group_ids": {
    "description": "内测资格群号",
    "type": "list",
    "default": [],
    "items": {
      "type": "string"
    },
    "hint": "Bot 必须在这些 NapCat/OneBot 群中。调用者只要仍在任一资格群，即可使用受保护指令。填写纯 QQ 群号。"
  },
  "protected_commands": {
    "description": "受保护指令",
    "type": "list",
    "default": [
      "群分析",
      "group_analysis"
    ],
    "items": {
      "type": "string"
    },
    "hint": "填写不带 / 的命令名；仅匹配消息的第一个命令词。例如 群分析 或 group_analysis。"
  },
  "membership_cache_seconds": {
    "description": "成员资格缓存秒数",
    "type": "int",
    "default": 300,
    "hint": "成功或失败的成员查询结果缓存时长。设为 0 表示不缓存；成员退群后的权限最多会延迟此秒数失效。"
  },
  "deny_mode": {
    "description": "拒绝方式",
    "type": "string",
    "options": [
      "silent",
      "message"
    ],
    "default": "silent",
    "hint": "silent=静默阻断；message=回复下方固定提示后阻断。"
  },
  "deny_message": {
    "description": "拒绝提示",
    "type": "string",
    "default": "该指令仅向内测群成员开放。",
    "hint": "仅 deny_mode=message 时使用。"
  },
  "auto_accept_group_invites": {
    "description": "内测好友自动同意群邀请",
    "type": "bool",
    "default": false,
    "hint": "开启后，仅当邀请人同时是 Bot 好友且仍在任一“内测资格群号”中，才自动同意其邀请 Bot 加入其他群。仅处理邀请（invite），不处理加群申请（add）；任一查询/API 失败均不批准。"
  },
  "disable_default_llm_response": {
    "description": "禁用普通对话 LLM 回复",
    "type": "bool",
    "default": false,
    "hint": "开启后，本插件会在 AstrBot 发起默认聊天 LLM 请求前静默阻断，因此自然语言对话不会调用 LLM。普通指令插件仍可执行；不影响群分析插件内部主动发起的分析任务。作用于此 NapCat Bot 的私聊和群聊。"
  }
}
